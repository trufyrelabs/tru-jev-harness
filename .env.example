# TypeSafe AI API key (never commit the real value).
# Create a key at https://console.typesafe.ai/settings/keys
TYPESAFE_API_KEY=

# Optional overrides (SDK defaults shown).
# TYPESAFE_BASE_URL=https://api.typesafe.ai
# TYPESAFE_DEFAULT_MODEL=jev-latest
# TYPESAFE_LOG_LEVEL=warn

# Force mock Jev answers without passing --mock.
# JEV_MOCK=1
